# Live VRM model

Place your production VRM model here as `companion.vrm`, then set:

```bash
VITE_ENABLE_LIVE_VRM=true
```

The site intentionally ships without a third-party avatar model so licensing stays under your control.
